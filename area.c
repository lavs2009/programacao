#include <stdio.h>
#include <stdlib.h>
main(){
    /*Fazer um aplicativo que receba do usuario a base a altura de um triangulo e devolva a area do triangulo retangulo.
    Formula da area do triangulo retângulo = Base*altura/2
    */
    int = base = 0;
    int = altura = 0;
 float  area = 0;
 printf("Digite a base do triangulo:\n");
 scanf("%d" ,&base);
 printf("Digite a altura do triangulo:\n");
 scanf("%d" ,&altura);
area = (base*altura)/2;
 printf("\n A area do triangulo retângulo e': %4.2f\n",area);
}

